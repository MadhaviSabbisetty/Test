export const FabStyle = styled(Fab)(({ theme }) => ({
  width: 48,
  height: 48,
  minWidth: 48,
  minHeight: 48,
  padding: 0,
  borderRadius: "50%",
  flexShrink: 0,

  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  flexDirection: "row",
  flexWrap: "nowrap",

  backgroundColor: theme.palette.primary.main,
  color: "#fff",
  border: `1px solid ${theme.palette.primary.main}`,

  transition:
    "width 0.3s ease, min-width 0.3s ease, border-radius 0.3s ease, background-color 0.3s ease",

  "& .create-circle-text": {
    opacity: 0,
    width: 0,
    marginRight: 0,
    whiteSpace: "nowrap",
    overflow: "hidden",
    flexShrink: 0,
    color: "#fff",
    transition:
      "opacity 0.2s ease, width 0.3s ease, margin-right 0.3s ease",
  },

  "&:hover": {
    width: 180,
    minWidth: 180,
    height: 48,
    borderRadius: "24px",

    justifyContent: "flex-start",

    paddingLeft: "14px",
    paddingRight: "14px",

    // Slightly translucent primary colour
    backgroundColor: alpha(theme.palette.primary.main, 0.82),
    border: `1px solid ${alpha(theme.palette.primary.main, 0.9)}`,
  },

  "&:hover .create-circle-text": {
    opacity: 1,
    width: 125,
    marginRight: "8px",
  },

  "& .MuiSvgIcon-root": {
    color: "#fff",
    flexShrink: 0,
  },
}));
