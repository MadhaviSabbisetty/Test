export const FabStyle = styled(Fab)(({ theme }) => ({
  // -----------------------------------------
  // NORMAL STATE
  // -----------------------------------------
  width: 48,
  height: 48,
  minWidth: 48,
  minHeight: 48,

  padding: 0,
  margin: 0,

  position: "relative",

  display: "flex",
  alignItems: "center",
  justifyContent: "center",

  borderRadius: "50%",
  overflow: "hidden",

  flexShrink: 0,
  boxSizing: "border-box",

  backgroundColor: theme.palette.primary.main,

  border: "none",
  boxShadow: "none",

  transition:
    "width 0.3s ease, border-radius 0.3s ease",

  // -----------------------------------------
  // DARK PURPLE PLUS CIRCLE
  // -----------------------------------------
  "&::after": {
    content: '""',

    position: "absolute",

    right: "-2px",
    top: "-2px",

    width: 48,
    height: 48,

    borderRadius: "50%",

    backgroundColor: theme.palette.primary.main,

    zIndex: 3,

    pointerEvents: "none",
  },

  // -----------------------------------------
  // PLUS ICON
  // -----------------------------------------
  "& .MuiSvgIcon-root": {
    position: "absolute",

    right: 11,
    top: 11,

    width: 24,
    height: 24,

    minWidth: 24,
    minHeight: 24,

    margin: 0,

    color: "#ffffff",

    zIndex: 4,

    flexShrink: 0,
  },

  // -----------------------------------------
  // CREATE SEGMENT TEXT
  // -----------------------------------------
  "& .create-circle-text": {
    position: "absolute",

    left: 16,
    right: 48,

    top: 0,

    height: 48,

    display: "flex",
    alignItems: "center",

    opacity: 0,

    width: "auto",
    maxWidth: "none",

    margin: 0,
    padding: 0,

    whiteSpace: "nowrap",
    overflow: "hidden",

    color: "#ffffff",

    fontSize: "14px",
    fontWeight: 500,

    lineHeight: 1,

    textAlign: "left",

    zIndex: 2,

    transition:
      "opacity 0.2s ease",
  },

  // -----------------------------------------
  // HOVER
  // -----------------------------------------
  "&:hover": {
    width: 180,
    minWidth: 180,

    height: 48,

    borderRadius: "24px",

    padding: 0,

    backgroundColor: "transparent",

    backgroundImage: `linear-gradient(
      to right,
      ${theme.palette.primary.main} 0%,
      ${theme.palette.primary.main} 30%,
      ${lighten(theme.palette.primary.main, 0.15)} 30%,
      ${lighten(theme.palette.primary.main, 0.15)} 60%,
      ${lighten(theme.palette.primary.main, 0.30)} 60%,
      ${lighten(theme.palette.primary.main, 0.30)} 100%
    ) !important`,

    border: `2px solid ${theme.palette.primary.main}`,

    boxShadow: "none",

    justifyContent: "flex-start",
  },

  // -----------------------------------------
  // SHOW CREATE SEGMENT
  // -----------------------------------------
  "&:hover .create-circle-text": {
    opacity: 1,
  },
}));
