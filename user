const CustomDataGridFooter = ({ onCreate }) => {
  return (
    <GridFooterContainer
      sx={{
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        px: 1,
      }}
    >
      {/* Create Circle Button */}
      <Box
        onClick={onCreate}
        sx={{
          display: "flex",
          alignItems: "center",
          justifyContent: "flex-end",
          height: 40,
          width: 40,
          borderRadius: "20px",
          backgroundColor: "transparent",
          cursor: "pointer",
          overflow: "hidden",
          transition: "width 0.25s ease",
          "&:hover": {
            width: 145,
            backgroundColor: "background.paper",
          },
          "&:hover .create-circle-label": {
            opacity: 1,
            width: "auto",
            marginRight: 8,
          },
        }}
      >
        <Typography
          className="create-circle-label"
          sx={{
            opacity: 0,
            width: 0,
            whiteSpace: "nowrap",
            overflow: "hidden",
            fontWeight: 500,
            transition: "opacity 0.2s ease",
          }}
        >
          Create Circle
        </Typography>

        <CircleTab1Fab
          color="primary"
          size="small"
          aria-label="Create Circle"
        >
          <CircleTab1Addicon />
        </CircleTab1Fab>
      </Box>

      {/* Pagination */}
      <GridPagination />
    </GridFooterContainer>
  );
};
