import React from "react";
import { styled, alpha } from "@mui/material/styles";
import {
  Box,
  Tabs,
  Fab,
  Grid,
  Chip,
  Stack,
  TextField,
} from "@mui/material";
import { DataGrid } from "@mui/x-data-grid";
import AddIcon from "@mui/icons-material/Add";

// segmentmaster

export const Boxdtatagrid = styled(Box)(({ theme }) => ({
  height: 500,
}));

export const BoxStyle = styled(Box)(({ theme }) => ({
  borderBottom: 1,
  borderColor: "divider",
}));

export const Box2Style = styled(Box)(({ theme }) => ({
  py: 2,
}));

export const TabsStyle = styled(Tabs)(({ theme }) => ({
  width: "100%",
}));

// segmentmastertab

export const StackStyle = styled(Stack)(({ theme }) => ({
  mt: 1.5,
}));

export const FabStyle = styled(Fab)(({ theme }) => ({
  width: 40,
  height: 40,
  minWidth: 40,
  minHeight: 40,
  padding: 0,
  borderRadius: "50%",
  flexShrink: 0,
  zIndex: 2,

  transition: "all 0.3s ease",

  "&:hover": {
    backgroundColor: theme.palette.primary.main,
  },
}));

export const CreateSegmentLabel = styled(Box)(({ theme }) => ({
  height: 40,
  maxWidth: 0,
  opacity: 0,
  overflow: "hidden",
  whiteSpace: "nowrap",

  display: "flex",
  alignItems: "center",

  padding: 0,
  marginLeft: 0,

  color: "#ffffff",
  backgroundColor: alpha(theme.palette.primary.main, 0.35),
  border: `1px solid ${theme.palette.primary.main}`,
  borderLeft: "none",

  borderRadius: "0 20px 20px 0",

  transition: "all 0.3s ease",
}));

export const CreateSegmentWrapper = styled(Box)(({ theme }) => ({
  display: "flex",
  alignItems: "center",

  "&:hover .create-segment-label": {
    maxWidth: 130,
    opacity: 1,
    paddingLeft: "12px",
    paddingRight: "12px",
  },
}));

export const AddIconStyle = styled(AddIcon)(({ theme }) => ({
  margin: 0,
  flexShrink: 0,
}));

export const GridContainer = styled(Grid)(({ theme }) => ({
  pt: 1,
}));

export const GridItem1 = styled(Grid)(({ theme }) => ({
  mt: 2,
  mb: 3,
}));

// export const Textfielss = styled(TextField)(({ theme }) => ({
//   mb: 90,
// }));

export const GridItem2 = styled(Grid)(({ theme }) => ({
  display: "flex",
  justifyContent: "flex-end",
  mt: 2,
}));

// mysegmentrequeststab

export const DataGridStyles = styled(DataGrid)(({ theme }) => ({}));

export const Chip1Style = styled(Chip)(({ theme }) => ({
  p: 0.5,
  m: 0,
  width: "90%",
}));
