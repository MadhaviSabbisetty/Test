import * as React from "react";
import PropTypes from "prop-types";
import { Tabs, Tab, Box, Paper } from "@mui/material";
import CurrencyRupeeIcon from "@mui/icons-material/CurrencyRupee";
import PendingActionsIcon from "@mui/icons-material/PendingActions";
import CurrencyRateChangeTab from "./CurrencyRateChangeTab";
import MyCurrencyRateRequestsTab from "./MyCurrencyRateRequestsTab";
import { CurrencyRateMasterStyles } from "./CurrencyRateChangeStyles";

function CustomTabPanel(props) {
  const { children, value, index, ...other } = props;

  return (
    <Box
      role="tabpanel"
      hidden={value !== index}
      id={`simple-tabpanel-${index}`}
      aria-labelledby={`simple-tab-${index}`}
      {...other}
    >
      {value === index && (
        <Box sx={CurrencyRateMasterStyles.customTabPanelBox}>{children}</Box>
      )}
    </Box>
  );
}

CustomTabPanel.propTypes = {
  children: PropTypes.node,
  index: PropTypes.number.isRequired,
  value: PropTypes.number.isRequired,
};

function a11yProps(index) {
  return {
    id: `simple-tab-${index}`,
    "aria-controls": `simple-tabpanel-${index}`,
  };
}

export default function CurrencyRateChange() {
  const [value, setValue] = React.useState(0);

  const handleChange = (event, newValue) => {
    setValue(newValue);
  };

  return (
     <Paper
    elevation={2}
    sx={{
      display:"flex",
      flexDirection:"column",
      borderRadius:2,
      overflow:"hidden",
      minHeight:"calc(100vh - 210px)",
      width:"100%",
    }}>
      <Box>
      <Box sx={CurrencyRateMasterStyles.tabBox}>
        <Tabs
          sx={CurrencyRateMasterStyles.tabs}
          value={value}
          onChange={handleChange}
          aria-label="basic tabs example"
          variant="fullWidth"
        >
          <Tab
            label="Manage Currency Rate"
            icon={<CurrencyRupeeIcon />}
            iconPosition="start"
            {...a11yProps(0)}
          />
          <Tab
            icon={<PendingActionsIcon />}
            iconPosition="start"
            label="My Requests"
            {...a11yProps(1)}
          />
        </Tabs>
      </Box>
      <CustomTabPanel value={value} index={0}>
        <CurrencyRateChangeTab />
      </CustomTabPanel>
      <CustomTabPanel value={value} index={1}>
        <MyCurrencyRateRequestsTab permissions={{view:true ,cancel:true}}/>
      </CustomTabPanel>
      </Box>
    </Paper>
  );
}import { styled } from "@mui/material/styles";
import {
  Box,
  Dialog,
  DialogActions,
  DialogContent,
  DialogTitle,
  TextField,
  Button,
  Grid,
  TableBody,
  TableCell,
  Table,
  TableContainer,
  TableHead,
  TableRow,
  FormControl,
  InputLabel,
  Select,
  MenuItem,
  CircularProgress,
  TablePagination,
  Chip,
  IconButton,
  Typography,
} from "@mui/material";
import { DataGrid } from "@mui/x-data-grid";
import { BorderBottom } from "@mui/icons-material";

export const StyledContainerBox = styled(Box)(({ theme }) => ({
  padding: theme.spacing(3),
}));

export const StyledTablePagination = styled(TablePagination)(({ theme }) => ({
  display: "flex",
  justifyContent: "space-around",
  borderTop: `1px solid ${theme.palette.divider}`,
  backgroundColor: theme.palette.background.paper,
  borderRadius: "0 0 12px 12px",
  "& .MuiTablePagination-toolbar": {
    minHeight: "48px",
  },
  "& .MuiTablePagination-selectLabel": {
    fontSize: "14px",
  },
  "& .MuiTablePagination-input": {
    fontSize: "14px",
  },
  "& .MuiTablePagination-displayedRows": {
    fontSize: "14px",
  },
  "& .MuiTablePagination-actions": {
    "& svg": {
      fontSize: "20px",
    },
  },
}));

export const StyledDataGridContainer = styled(DataGrid)(({ theme }) => ({

}));

export const StyledDialog = styled(Dialog)(({ theme }) => ({
  "& .MuiDialog-paper": {
    minWidth: "500px",
    // height: "520px",
    display: "flex",
  },
}));

export const StyledDialogTitle = styled(DialogTitle)(({ theme }) => ({
  marginTop: theme.spacing(1),
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
}));

export const StyledDialogContent = styled(DialogContent)(({ theme }) => ({}));

export const StyledTextField = styled(TextField)(({ theme }) => ({
  marginTop: theme.spacing(1),
  marginBottom: theme.spacing(1),
}));

export const StyledDialogActions = styled(DialogActions)(({ theme }) => ({
  padding: "16px 24px",
  justifyContent: "flex-end",
}));

export const StyledCircularProgressBox = styled(Box)(({ theme }) => ({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  height: 300,
}));

export const StyledCircularProgress = styled(CircularProgress)(
  ({ theme }) => ({})
);

export const StyledButton = styled(Button)(({ theme }) => ({}));

export const StyledGridContainer = styled(Grid)(({ theme }) => ({
  marginBottom: theme.spacing(2),
}));

export const StyledGridItem = styled(Grid)(({ theme }) => ({
  // Note: Spacing is usually handled by Grid 'spacing' prop or 'item'
  // Adding marginRight here might break Grid layouts, use carefully.
  // marginRight: theme.spacing(2), 
}));

export const StyledFormControl = styled(FormControl)(({ theme }) => ({}));

export const StyledInputLabel = styled(InputLabel)(({ theme }) => ({}));

export const StyledSelect = styled(Select)(({ theme }) => ({}));

export const StyledMenuItem = styled(MenuItem)(({ theme }) => ({}));

export const StyledTableContainer = styled(TableContainer)(({ theme }) => ({
  borderRadius: "10px",
}));

export const StyledTable = styled(Table)(() => ({}));

export const StyledTableHead = styled(TableHead)(() => ({}));

export const StyledTableBody = styled(TableBody)(() => ({}));

export const StyledTableRow = styled(TableRow)(() => ({
  "&:hover": {},
}));

export const StyledTableHeaderCell = styled(TableCell)(({ theme }) => ({
  fontWeight: "bold",
  fontSize: "14px",
}));

export const StyledTableCell = styled(TableCell)(() => ({
  fontSize: "14px",
}));

export const StyledEmptyCell = styled(TableCell)(({ theme }) => ({
  textAlign: "center",
  padding: theme.spacing(3),
  color: theme.palette.text.secondary,
}));

export const StyledCloseButton = styled(IconButton)(() => ({
  padding: 4,
  // alignContent is not for text alignment. 
  // IconButton handles centering usually.
}));

export const StyledBox = styled(Box)(({ theme }) => ({
  marginTop: theme.spacing(2),
}));

export const StyledTypographyBox = styled(Box)(({ theme }) => ({
  marginBottom: "20px",
}));

export const StyledDataGrid = styled(DataGrid)(({ theme }) => ({
  
}));

export const StyledDialogTitleBox = styled(Box)(({ theme }) => ({}));

export const StyledTypography = styled(Typography)(({ theme }) => ({
  fontWeight: "bold",
  fontSize: "20px",
}));

export const StyledDateChip = styled(Chip)(({ theme }) => ({
  color: theme.palette.text.secondary,
}));

export const StyledStatusChip = styled(Chip)(({ theme }) => ({
  padding: theme.spacing(0.5),
  margin: 0,
  width: "90%",
}));

export const StyledStatusBox = styled(Box)(({ theme }) => ({
}));

export const StyledRequestDataGrid = styled(DataGrid)(({ theme }) => ({

}));

export const StyledRequestContainerBox = styled(Box)(({ theme }) => ({}));

export const CurrencyRateMasterStyles = {
  tabBox: {
    borderColor: "divider",
  },
  tabs: {
    width: "100%",
    height: "100%",
  },
  customTabPanelBox: {
    // py: 2,
  },
};
