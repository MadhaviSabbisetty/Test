package com.fincore.enquiry_service.service;

import com.fincore.enquiry_service.dto.DifferenceDTO;
import com.fincore.enquiry_service.dto.DifferenceHistoryDTO;
import com.fincore.enquiry_service.dto.DifferenceSearchDto;
import org.springframework.data.domain.Page;

public interface IDifferenceService {
    Page<DifferenceDTO> getAllDifferences(int page, int size);

    Page<DifferenceDTO> searchDifferences(DifferenceSearchDto criteria, int page, int size);

    Page<DifferenceHistoryDTO> getGlccHistory(String branchCode, String cgl, int page, int size);
}
