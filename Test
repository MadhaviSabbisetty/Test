renderGroup={(params) => (
    <React.Fragment key={params.key}>
      {params.children}

      {branchHasMore && (
        <li
          style={{
            textAlign: "center",
            padding: "10px",
            cursor: "pointer",
            fontWeight: 600,
            color: "#1976d2",
          }}
          onMouseDown={(e) => {
            // 👈 THIS IS CRITICAL
            e.preventDefault();
          }}
          onClick={() => {
            fetchBranches(circle, branchSize + 20);
          }}
        >
          {branchLoading ? "Loading more…" : "⬇ Show more branches"}
        </li>
      )}
    </React.Fragment>
